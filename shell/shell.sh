#!bin/bash
#sort默认升序，-r 降序
#cat file.txt | sort -r > data.txt.sorted
#-u 去重
#cat file.txt | sort -u
#-n指定使用按照数值进行排序,默认是按照字典序
#cat file.txt | sort -n
#指定分隔符，指定域进行排序
#cat file.txt | sort -n -r -k 2 -t
#uniq 相邻行去重
#echo "aaa
#bbb
#bbb
#ccc
#bbb" | uniq -c


